apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sentry.fullname" . }}-test-connection"
  labels:
    {{- include "sentry.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sentry.fullname" . }}:{{ .Values.service.externalPort }}']
  restartPolicy: Never
